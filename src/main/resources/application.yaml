spring:
  datasource1:
    url: jdbc:sqlserver://localhost;databaseName=application
    username: SA
    password: password
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
  datasource2:
    url: jdbc:sqlserver://localhost;databaseName=users
    username: SA
    password: password
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    show_sql:
      hibernate:
        ddl-auto: update
    generate-ddl: true
    show-sql: true
    database: sql_server
